schema: "../schema/dist/server/**/*.graphql"
generates:
  src/agent/agent.generated.graphql.ts:
    documents: "src/server/agent/agent.graphql"
    schema:
      - "src/server/schema/*.core.graphql"
    plugins:
      - add:
          content: "/* eslint-disable */"
      - typescript
      - typescript-graphql-request
      - typescript-operations
    config:
      enumsAsTypes: true
      avoidOptionals: true
      rawRequest: true

  src/server/schema/index.ts:
    schema: "src/server/schema/*.core.graphql"
    plugins:
      - add:
          content: "/* eslint-disable */"
      - typescript
      - typescript-resolvers
      - typescript-operations
      - typed-document-node
    config:
      enumsAsTypes: true
      avoidOptionals: true
      useIndexSignature: true
      defaultMapper: DeepPartial<{T}>
      contextType: ../context#ConnectorContext
      scalars:
        DateTime: Date
        Void: void
