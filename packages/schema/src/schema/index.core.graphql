scalar Date
scalar Void
scalar JSONObject

interface PluginBase {
  id: String!
}

type CorePlugin implements PluginBase {
  id: String!
}

union Plugin = CorePlugin

type Query {
  plugins: [Plugin!]!
}

type Subscription {
  version: String!
}

type Mutation {
  version: String!
}
